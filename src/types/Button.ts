export interface SortButtonsTypes {
  active: string | null;
  ascendant: string;
  descendant: string;
}

export interface ButtonTypes {
  theme: string;
  active?: boolean;
}

export interface BgColors {
  primary: string;
  sencondary: string;
  ligth: string;
  dark: string;
  transparent: string;
}
